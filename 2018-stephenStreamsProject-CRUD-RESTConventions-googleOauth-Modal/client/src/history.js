/*iK created our own custom history object in <Router> instead of using the <BrowserRouter> history object. */

import createHistory from 'history/createBrowserHistory';

export default createHistory();
